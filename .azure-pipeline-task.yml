parameters:
  vstsTasks: []

steps:
- ${{ each vstsTask in parameters.vstsTasks }}:
  - script: |
      npm install
    workingDirectory: ${{ vstsTasks }}/v2
    displayName: npm install

  - script: |
      npm run build
    workingDirectory: ${{ vstsTasks }}/v2
    displayName: npm run build